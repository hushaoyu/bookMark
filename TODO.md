** cmd
- git subtree push --prefix dist origin gh-pages

~~增加按标签统计展示数量~~
~~重构应用，组件化开发~~
~~index.css 文件太大，参考之前的组件化进行分割~~
~~编辑器里面，tsx文件很多ts相关的告警，需要配置tsconfig.json 里面的 "noImplicitAny": false,~~
~~链接列表，不展示标签信息，按钮只展示文本，不展示背景框，保持一行展示，减少空间占用~~
~~功能扩展，增加备忘录功能，用户可以在应用内添加、查看和管理备忘录~~
~~这个应用，有性能或者安全方面的缺陷吗？如果有，你会如何解决？~~
~~增加备忘录功能，和增加安全和性能优化改造后，现在导入导出功能是否齐全正常，是否有遗漏的地方？~~
~~增加自动检查更新功能，用户可以在应用内设置是否自动检查检查更新，设置为是时，每次打开应用，会检测应该是否有更新，是则提示用户更新~~
~~备忘录编辑功能，能否提供一些排版功能，比如缩进、列表等~~
~~查看应用缓存数据大小：总大小，每个缓存项的大小~~
~~导入导出功能优化：导入时，可提供选项，是覆盖当前数据，还是合并导入 - 【合并导入时，如果是链接，则以url作为唯一标识，其他字段数据合并，避免重复导入。如果是备忘录时，以备忘录title作为唯一标识，其他字段数据合并，避免重复导入】~~
~~链接管理，增加单个记录的链接复制功能，用户可以点击链接，复制链接地址到剪贴板~~
- 通用
    1、导出功能，增加选项，导出所有数据，包括链接、备忘录、记账记录，或者用户可以选择导出的类型，比如仅导出链接、仅导出备忘录、仅导出记账记录。
- 记账本
    1、移除假数据
    2、收入、支出添加记账页面，以及首页记账列表，里面的图标和分类名称，都调小一点，手机屏幕本来就小
    3、记账记录列表，按天聚合展示，倒序展示，除了展示当天的每条记录项，还需要统计当天的支出
    4、首页展示的月份，用户可以通过点击月份切换，切换到其他月份的记账记录
        切换月份的功能交互优化，现在单纯左右点击切换上一个月，下一个月，不方便
    5、增加预算配置功能
    6、添加记账记录时，目前只支持一层的加减运算，需要增加多层运算的支持，比如：100元收入，减去20元支出，再减去10元支出，等于70元余额。
